import { one } from '..'

describe('Symbol', () => {
    it('setup Symbol asyncIterator shim', () => {
        // console.log(Symbol.asyncIterator)
        // t.ok(Symbol.asyncIterator)
        // t.end()
    })
})

